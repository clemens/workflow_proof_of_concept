<%= form_for(order, url: transition_order_path(order), method: :put) do |f| %>
  <%= hidden_field_tag :id, order.try(:id) %>
  <%= hidden_field_tag :transition, :place %>

  <%= f.submit 'Place' %>
<% end %>
